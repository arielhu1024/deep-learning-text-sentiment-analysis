# Deep Learning for Fine-Grained Sentiment Analysis

This project investigates fine-grained sentiment classification using the SST-5 dataset and compares the performance of recurrent neural networks (LSTM) and transformer-based language models.

The study evaluates how different deep learning architectures capture semantic information and emotional nuances in text classification tasks.
